# Forge-Detect
This project is a deep learning-powered web application for detecting forged or tampered images. Built using Flask and TensorFlow/Keras, the app allows users to upload an image, which is then analyzed by a pre-trained CNN model to classify it as authentic or tampered.
